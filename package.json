{
  "name": "profound-codex-workshop",
  "version": "1.0.0",
  "private": true,
  "description": "Synthetic Profound-flavored Codex workshop repo: harness engineering, PR gates, skills, hooks, and agent-native context.",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "start": "tsx backend/server.ts",
    "test": "node --import tsx --test backend/server.test.ts services/campaign-export.test.ts services/csv-export.test.ts services/visibility-service.test.ts",
    "lint": "tsx tools/lint/check-static.ts && tsx tools/lint/check-boundaries.ts",
    "gate": "npm run build && npm run lint && npm test && npm run e2e",
    "e2e": "tsx e2e/playwright-optional.ts",
    "session-mine": "tsx tools/session-mining/mine-session.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.7.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "typescript": "^5.9.3",
    "vite": "^6.4.1"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "tsx": "^4.22.4"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  }
}
