rules:
  - id: no-raw-fetch-outside-api-client
    message: "Frontend code must use src/api/client.ts instead of raw fetch."
    severity: ERROR
    languages: [typescript, javascript]
    pattern: fetch(...)
    paths:
      include:
        - "frontend/src/**/*.ts"
        - "frontend/src/**/*.tsx"
      exclude:
        - "frontend/src/api/client.ts"
