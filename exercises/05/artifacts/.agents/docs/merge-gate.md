# Merge Gate

The gate combines build, static checks, architecture boundaries, behavior tests, and browser/HTTP proof.

Run the narrowest relevant command first, then `npm run gate`.

Green deterministic checks do not approve customer-facing design intent. Human review still owns brand and customer impact.
