# Exercise 01: Build The Foundation

Start from `demo-0/bad-state`. Codex has no shared Profound-specific harness, so each run can rediscover brand, review, API, and testing expectations differently.

Fix by adding the AGENTS hierarchy and canonical docs that tell Codex where product, harness, and human-review rules live.

Harness lesson: horizontal repo context beats per-engineer memory. Instructions and docs travel with the code and bind whichever agent an engineer chooses.
