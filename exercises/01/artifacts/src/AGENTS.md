# Frontend Instructions

- Fetch backend data only through `src/api/`.
- Put reusable visual decisions in `src/design-system/`.
- Customer-facing color, copy, and layout changes require explicit human review.
