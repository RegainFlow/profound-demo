# Frontend Instructions

- Fetch backend data only through `frontend/src/api/`.
- Put reusable visual decisions in `frontend/src/design-system/`.
- Customer-facing color, copy, and layout changes require explicit human review.
