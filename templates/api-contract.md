# API Contract Template

- Frontend integrations live in `frontend/src/api/`.
- Raw HTTP calls are wrapped by one client module.
- Feature code consumes typed API functions.
- Backend data is synthetic until a human explicitly approves real-system integration.
