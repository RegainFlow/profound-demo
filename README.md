# Profound Demo 1: Foundation

This branch adds the first repo-local agent context: root and project-specific `AGENTS.md` files plus the visibility scoring policy. The app still uses weak tests and has no lint, scan, or full gate yet.

## Verify

```powershell
npm test
npm run build
```
