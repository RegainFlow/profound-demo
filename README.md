# Profound Demo 6: Lifecycle Hook

This branch keeps the full gate from demo 5 and adds only the Codex hook configuration template. The hook itself is discussed live; no hook doc is added to the repo.

## Verify

```powershell
npm run gate
```
