# Profound Demo 7: Flywheel

This branch adds the session-mining tool, session-mining template, and `skill-spotter` skill. The lesson is to promote repeated corrections into the smallest durable artifact: rule, doc, skill, hook, or nothing.

## Verify

```powershell
npm run gate
npm run session-mine
```
