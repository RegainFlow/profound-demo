# Profound Demo 2: Behavioral Tests

This branch adds the `write-tests` skill and focused visibility scoring tests. The premature rounding bug is fixed, but architecture lint, Semgrep, and the full PR gate have not been introduced yet.

## Verify

```powershell
npm test
npm run build
```
