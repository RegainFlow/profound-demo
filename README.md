# Profound Demo 4: Feedback To Rule

This branch shows a repeated review comment becoming a durable rule. It keeps the architecture boundary checks and adds the premature visibility rounding Semgrep rule plus the feedback-to-rule template.

## Verify

```powershell
npm run lint
npm run scan
npm test
npm run build
```
